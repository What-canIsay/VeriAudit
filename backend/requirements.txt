fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
litellm>=1.40
python-multipart>=0.0.9
jinja2>=3.1
# Offline SAST fallback for the semgrep_scan tool (pure-Python, no network).
bandit>=1.7
# Multi-language call graph + cross-procedure reachability (app/callgraph.py).
tree-sitter>=0.21
tree-sitter-language-pack>=0.2
# RAG semantic code retrieval (app/rag/*): numpy for the vector store (required).
numpy>=1.24
# fastembed = real OFFLINE semantic embeddings (ONNX, no torch); recommended. Without it,
# RAG auto-falls back to the deterministic lexical `hashing` backend (no extra deps).
fastembed>=0.3
